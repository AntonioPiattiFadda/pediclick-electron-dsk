// Delivery order state has been moved to Redux — see src/stores/deliveryOrderSlice.ts
// Mutations (addItemToOrder, removeItemFromOrder, updateOrderClient) live in the
// components that use them (PricingPanel, DeliveryCart, DeliveryOrder).
